<h1>Bonjour new doses</h1>
<%= simple_form_for @cocktail do |f| %>
  <%= f.input :name %>
  <%= f.association :ingredients, collection: Ingredient.all %>
  <%= f.association :doses %>
  <%= f.button :submit %>
<% end %>
